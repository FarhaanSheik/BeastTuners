@model IEnumerable<BeastTuners.Models.PartSupplier>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Part)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Supplier)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Part.Category)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supplier.Address)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PartSuppliersID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PartSuppliersID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PartSuppliersID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
